Run with `npm test`. The suites check the two acceptance criteria that are
numbers rather than judgement: the stall markup distribution (§7.2, milestone 4)
and the reference-build triangle (§9.7, milestone 9).
